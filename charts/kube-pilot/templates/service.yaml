apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-pilot.fullname" . }}
  namespace: kube-pilot
  labels:
    {{- include "kube-pilot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-pilot.selectorLabels" . | nindent 4 }}
